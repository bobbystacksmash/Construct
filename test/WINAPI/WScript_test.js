const assert = require("chai").assert;
const WScript = require("../../src/winapi/WScript");

let context = {
    epoch: 1,
    emitter: { emit: () => {} },
    exceptions: {},
    vfs: {}
};

describe("WScript", () => {

    describe("Properties", () => {

    });

    describe("Methods", () => {

    });

});
